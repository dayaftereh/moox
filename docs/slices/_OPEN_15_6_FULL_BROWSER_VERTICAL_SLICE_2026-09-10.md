# Planned slice 15.6 - Full browser vertical slice / polish / complete-game QA
Status: **open; Gate 2 frozen; Gate 3 implementation ready**.
Queue position: **15.6 of Slice-15 family**.
## Objective
Integrate and polish Slices 15.1-15.5 into the first complete browser-playable Master of Orion X vertical slice: Main Menu -> New Game -> strategic play -> save/reload/resume -> supported decisions and interactive Tactical battle -> victory.
## Dependencies
- Slices 15.1-15.5 closed.
- Slice 13 built-in AI.
- Slice 14 live persistence.
## Scope guard
In scope:
- coherent Main Menu / New Game / Resume / active-game / result journey;
- complete supported Human-vs-built-in-AI playthrough using browser surfaces only;
- final navigation consistency and visual polish across all implemented screens;
- integration of 2D Galaxy/system/Colony/Fleet/Research/Diplomacy surfaces with rich decisions/persistence and Slice-15.5 interactive Tactical Combat;
- loading/performance/runtime-error cleanup that blocks normal play;
- save/reload/reconnect smoke and recovery paths;
- representative responsive support for agreed viewports;
- E2E/browser smoke coverage for the canonical match journey;
- final authority audit proving React contains presentation/orchestration only.
Defer:
- Slice 16 New Game/preset-race breadth;
- Slice 17 broad military Ship Designer breadth;
- Tactical breadth beyond the explicit Slice-15.5 accepted baseline;
- advanced Diplomacy/Espionage/leader/alternative-victory systems beyond authoritative baselines explicitly accepted by preceding slices;
- native packaging and large-scale content production beyond the canonical vertical slice.
## Gate 1 - Integration readiness audit
- [x] Audit all 15.1-15.5 acceptance evidence and unresolved cross-slice UX seams.
- [x] Define the exact canonical browser playthrough and checkpoints from launch to victory, including at least one supported interactive Tactical battle path where the canonical scenario provides one.
- [x] Identify blockers requiring integration fixes rather than new gameplay scope.
- [x] Define performance/responsive/error thresholds for a playable vertical slice.
- [x] Define automated/manual browser E2E strategy including save/reload/reconnect and strategic <-> Tactical transitions.
- [x] Present Gate-2 final vertical-slice acceptance contract.
## Gate 2 - Vertical-slice freeze
- [x] Freeze canonical start-to-victory browser scenario and allowed setup.
- [x] Freeze final navigation/polish/performance acceptance thresholds.
- [x] Freeze save/reload/reconnect and failure-recovery acceptance cases.
- [x] Freeze browser/E2E evidence requirements and no-dev-tool rule.
- [x] Freeze expected strategic -> Tactical -> strategic integration checkpoints.
## Gate 3 - Integration and polish
- [ ] Close cross-screen/navigation/data-refresh seams.
- [ ] Complete Main Menu/New Game/Resume/result integration.
- [ ] Integrate and polish the accepted interactive Tactical path inside the complete strategic journey.
- [ ] Fix blocking performance/responsive/runtime-error issues.
- [ ] Add E2E/smoke automation where practical and deterministic.
- [ ] Perform complete Human-vs-built-in-AI browser playthroughs without direct API/dev-tool commands.
## Gate 4 - Final milestone QA + close
- [ ] Complete at least one full supported Human-vs-built-in-AI browser game from Main Menu to victory.
- [ ] Exercise at least one supported interactive Tactical battle entirely through the browser when present in the frozen canonical scenario.
- [ ] Save mid-game, reload/reconnect and finish with authoritative deterministic continuation.
- [ ] Verify representative responsive/browser-runtime behavior and production asset loading.
- [ ] Run full Go tests/vet/web build/E2E plus `git diff --check`.
- [ ] Perform final React authority scan and document milestone evidence.
- [ ] Update parent Slice-15 milestone, status/HISTORY and close marker.
Milestone on closure: **first saveable, resumable and finishable Human-vs-built-in-AI MOOX browser vertical slice with an integrated interactive Tactical Combat path**.

## Gate-1 evidence (2026-09-10)

Gate 1 is complete. Evidence: docs/research/SLICE_15_6_GATE1_INTEGRATION_READINESS_AUDIT_2026-09-10.md. Gate-2 draft: docs/research/SLICE_15_6_GATE2_VERTICAL_SLICE_CONTRACT_DRAFT_2026-09-10.md. Gate 3 remains blocked until explicit user acceptance/freeze.

## Gate-2 freeze (2026-09-10)

Explicitly accepted by user after Ship Designer UX refinements. Frozen evidence: docs/research/SLICE_15_6_GATE2_FREEZE_2026-09-10.md. Gate 3 may now implement in small checkpointed/recovered blocks.

## Gate-3 Block 5 plan - Galaxy fleet direct manipulation (2026-09-10)

User feedback identified fleet movement from the Galaxy map as the next integration blocker. The current home-system markers are colony presence plus aggregate own-fleet ship count, but the fleet marker is not independently interactive. The existing Fleets view can stage `empire.move_fleet` only when server-projected legal targets exist.

Planned Block 5 is now documented in `docs/research/SLICE_15_6_GATE3_BLOCK5_GALAXY_FLEET_MOVEMENT_PLAN_2026-09-10.md`:

Block 5A COMPLETE (`0f6c262`, `0588a9f`, `850350e`): persistent visited-system and known-empire authority, anonymous unvisited stars, hidden planets/special identity, pre-contact diplomacy/seat/contact filtering, server-side first-contact persistence, player-safe built-in AI exploration, full Go regression green, web build green, and visible canonical browser QA proving 1 named home + 19 anonymous stars + no pre-contact Diplomacy contact.

Block 5B COMPLETE (`09a5b03`): authoritative legal + visible-illegal fleet-target catalog with direct distance, supply distance, range, ETA and stable rejection reason; existing legal `fleet_moves` now derive from this same authority. Focused 2 pc legal / 5 pc out-of-range fixture plus command-validation parity and anonymous-target session coverage are green; full Go regression and web build are green. Next: 5C Galaxy fleet marker + floating picker.
Block 5C COMPLETE (`3338ead`): independent Galaxy fleet marker + compact draggable fleet/ship picker, exact persisted ship SVGs, whole/single-ship selection profiles backed only by `fleet_move_targets`, fixed Colony Ship support handling, and visible browser QA for marker/system-dialog separation, selection, dragging and canonical 0/19 reachable feedback. Full Go regression and web build are green. Next: 5D drag/drop + tap target mode, authoritative route/range feedback and staged movement.
5C UX refinement (`3503a6b`, corrected by `7131046`): replaced fleet tabs with one compact four-column system-ship pool, vertical scroll, stable Colony/Outpost/Transport vector silhouettes, green own-colony system names, and **one player-colored fleet marker per empire/system** rather than per internal fleet. Canonical QA: Scout 1 + Scout 2 + Colony Ship together, 292 px picker, 2/3 cross-fleet selection, 0/19 authoritative targets. Next remains 5D.
Block 5D COMPLETE (`ee9f60e`): compact `Ziel wählen` + direct picker drag/drop onto Galaxy stars, green legal/red blocked target rings from authoritative `fleet_move_targets`, anonymous server-derived distance/range/ETA rejection feedback, and legal per-source `empire.move_fleet` draft staging using projected `ship_ids`. Canonical `0x8009` visible QA proves 19 blocked targets, no name/dialog leak, no draft on rejection, equivalent drag rejection, and empty-space reposition. Positive legal movement acceptance follows immediately in 5E's deterministic 2 pc/5 pc fixture.
- persistent per-empire visited-system knowledge and player-safe Galaxy projection: unvisited stars expose position + stellar appearance only (no true name, planets or special identity such as Orion); arrival reveals and names the system; unknown race/diplomacy identity remains hidden until authoritative first contact;
- authoritative legal + visible-illegal fleet target projection with range/ETA/reason feedback;
- independently clickable Galaxy fleet marker;
- compact draggable fleet picker with selectable exact ship SVGs;
- drag/drop-to-star plus tap/click `Ziel wÃƒÂ¤hlen` fallback;
- deterministic QA-only three-star micro fixture (`2 pc` reachable target, `5 pc` out-of-range target) beside, not instead of, canonical seed `0x8009`;
- visible-browser movement -> encounter -> Tactical regression, including Laser fire and strategic return.

The normal Small-Galaxy generator and canonical `0x8009` seed are explicitly not to be altered merely to make this focused QA case reachable.
Special-ship visual persistence remains OPEN: Colony/Outpost/Troop strategic units need authoritative persisted per-instance visual genomes with deterministic creation/backfill and save/load/reconnect stability; the current `SpecialShipGlyph` SVGs are interim presentation only.
5D UX refinement (`def7af0`) supersedes the explicit `Ziel wählen` step: opening the one shared fleet picker immediately displays server-authoritative green/red destination states; ship selection refreshes those states live; sibling `?` controls open ship details without changing selection; and System `Flotten / Schiffe` opens that same compact picker instead of the legacy large modal. Positive legal-move proof remains Block5E.
Block5D refinement (`43e8035`, `f6fc6a5`): legacy System Fleet modal source removed. Colony/Outpost/Troop `?` details now show effective drive + FTL and Fuel Cell + parsec range from the same server rules used for movement; fuel upgrades remain dynamic for existing special fleets. Next remains Block5E positive legal-move/arrival/contact/Tactical acceptance.
Block5E durable reference setup (`819880a`, `52523b2`): keep `game-1` (`0x8009`, generated 20-star, Human + Darlok AI, initial 0/19 reachable) and `game-triangle-2pc` (`triangle-2pc-v1`, `0x800A`, Human + Darlok AI + Psilon AI, three symmetric homes, every edge authoritative 2 pc, initial 2/2 legal ETA1) side-by-side via the explicit dev-only `-reference-games` bootstrap. This supersedes the single throwaway 2pc/5pc micro-fixture design and becomes the standing QA pair for future slices.
Block5D route lifecycle is now frozen: planned movement is editable only before `Fertig`; green dashed planned routes retarget with the draft, source click cancels the draft, and blocked stars show a red dashed diagnostic without staging an order. After turn submission an in-transit Fleet cannot be cancelled or redirected; its green transit route/marker is status-only and exposes server-projected remaining pc + ETA until arrival.
Fleet-marker anchored routes/read-only transit inspection are accepted: planned/blocked lines begin at the own Fleet symbol, while a committed in-transit Fleet symbol opens the compact Fleet dialog with remaining pc/turns/ETA and per-ship detail inspection. Transit ship selection and all flight mutation controls are intentionally absent.
**Block5F FOREIGN FLEET / SCANNER FOLLOW-UP QUEUED after Block5E.** Server-authoritative scanner coverage governs whether foreign Fleets in transit exist in the player's projection. Detected incoming/hostile contacts reuse the Galaxy transit language: foreign player-colored Fleet marker, red dashed route toward any scanner-authorized/known target, projected remaining pc/ETA, and the compact read-only Fleet dialog with scanner-redacted ship intelligence. No hidden foreign Fleet state may be sent to React. Research/freeze exact scanner stacking, station/ship coverage sources, size-class extension, last-known-contact behavior and deep-space contact grouping before implementation. Detailed plan: `docs/research/SLICE_15_6_GATE3_BLOCK5F_FOREIGN_FLEET_SCANNERS_PLAN_2026-09-12.md`.
**Laser Scout queue regression fixed (`5c2c569`).** Triangle Psilon AI previously sent an illegal concrete `technology_id` for an all-applications research field, so the resolver rejected the turn after `Fertig` and left the session in `strategic_resolution`; the construction queue only appeared empty because planning preview was no longer active. AI research payloads now honor `ResearchSelectionMode`. Host + browser acceptance covers Scout Rev.2 with Laser queued on Human Home and successful transition to Round 2 Planning with construction progress preserved.
**Block5E COMPLETE - CO-LOCATED FOREIGN FLEET / TACTICAL ENTRY (`3b2d734`, user browser acceptance 2026-09-12).** Keep Triangle starting Scouts unarmed. After contact, foreign Fleets stationary in a visited shared system are clickable/read-only inspectable from the Galaxy marker; their combat Ships and special vessels can be inspected with `?`, while they never enter the player own-Fleet authority lists. `Angreifen` lives in this dialog and performs the existing authoritative war declaration; the Battle is created on the next `Fertig`. Tactical no longer has the 2v2 ceiling, accepts unarmed combat Ships and 3v2+, ignores civilian Fleet/colony context for battlefield materialization, and provides `Rückzug`. Colony/Outpost/Troop transports remain strategic context only and never appear as Tactical ships. Block5F still owns scanner/deep-space foreign Fleet detection and intelligence redaction. The user has now played the Triangle scenario through the previously blocked larger-Fleet encounter and successfully reached the Battle/Tactical UI, closing Block5E. Active review returns to 5A Tactical UI before Block5F implementation

**TACTICAL UI REFINEMENT BLOCK1 IMPLEMENTED / BROWSER-QA GREEN (user shorthand 5A Tactical UI).** Battle/Tactical now has an immersive full-viewport shell: only the floating game menu remains from the strategic chrome; resource chips, strategic turn/phase, side navigation and strategic bottom `Fertig` are hidden. Entered Tactical replaces the encounter card with the battlefield, uses 1x1 movement cells with stronger 4x4 grid lines, renders only server-projected legal destinations in green, and adds a bottom own-Ship/action HUD with Move/Fire/Scan, weapon selection, `Nächstes Schiff`, `Rückzug` and Fleet-style Scan details. Browser QA replayed the real Triangle path on parallel port 7172 without resetting the user live 7171 3v2 encounter. Authority guard: move/fire still submit immediately under the existing Battle command API; staged activation commit and no-selection=`fire all` require a later server-authority contract and are intentionally not faked in React.


**TACTICAL UI REFINEMENT BLOCK2 IMPLEMENTED / BROWSER-QA GREEN.** Tactical normal interaction no longer has explicit Move/Fire modes: server-legal green cells are always directly clickable for movement and server-legal enemy Ships are directly clickable for the selected authoritative weapon; Scan is the only explicit temporary interaction mode. Bottom HUD is reduced (~151px -> ~94px at 791x605 QA), 1x1 minor/4x4 major grid strokes are materially stronger, visible +/-/recenter camera controls are removed, and camera interaction is wheel zoom plus left/right drag, one-finger pan and two-finger pinch. Click-vs-drag suppression prevents accidental move/fire after panning. Browser QA cloned the exact preserved Round6 3v2 7171 snapshot read-only onto isolated 7173: direct enemy click produced beam_fired and 3 armor damage; direct green-cell click produced ship_moved 20->19; left/right drag, wheel, touch pan/pinch and Scan toggle were all verified. Canonical 7171 Battle was not mutated by destructive QA. Deferred activation batching and no-selection=fire-all remain authority follow-ups.

**TACTICAL UI REFINEMENT BLOCK3 IMPLEMENTED / BROWSER-QA GREEN.** Tactical ship presentation now reuses strategic visual identity instead of a Tactical-only seed: `TacticalShipSpec/View` carry source design identity and optional persisted visual revision/genome; persisted genomes are validated/deep-cloned through materialization/session/view, while legacy ships use the same Fleet/System design fallback seed. Field art is clipped to exactly one 1x1 cell and uses shared hull footprints (Frigate .48 through Doom Star 1.0). The permanent grid is no longer rendered. Clicking the active own Ship or its full-cell hit area toggles movement selection: current cell blue, only server-projected legal destinations green, every other occupied Ship cell red. Browser QA on isolated 7174 showed 0 overlays before selection, then 1 blue + 514 green + 4 red; no red coordinate was green and diagonal legal cells around blockers remained available. The newly built Scout used persisted seed `shipbuilder:catalog:20:1`; legacy starting Scouts in the preserved old save have no persisted genome but now reproduce Fleet/System design-seeded SVGs. Canonical 7171 was not modified.

**TACTICAL UI REFINEMENT BLOCK4 IMPLEMENTED / BROWSER-QA GREEN.** Server `active_ship_id` is now the sole activation authority in React: own activation immediately shows the active cell blue, every server-projected legal destination green and every other occupied Ship cell red, with no second local selection click. Camera recenters automatically when active Ship changes. Battlefield Ship-name text is removed; playfield and own-Ship strip are SVG-first, with the strip reduced to icon-only buttons and active/complete/destroyed state styling. New `beam_fired` events produce a short source-to-target beam/impact animation and `ship_moved` events produce a short authoritative movement trail/arrival pulse. Isolated 7174 QA proved immediate 1 blue + 514 green + 4 red on Tactical entry, a hit beam carrying damage=3, movement (10,13)->(11,13) with trail and legal-move recomputation, then `Nächstes Schiff` advanced to Round3 Ship23 at (14,12) with blue/green/red overlays and camera following automatically. Canonical 7171 remained untouched.

**PLANNING-DRAFT RELOAD/RECONNECT PERSISTENCE IMPLEMENTED / BROWSER-QA GREEN.** The former React-only `draftOrders` seam is closed for hosted browser reload/reconnect: per-seat strategic planning drafts now live on the server keyed by game/turn/base revision with monotonic `draft_revision`, exact replacement keys/payloads, authoritative PlanningPreview validation, stale-save suppression, snapshot rehydration and automatic clear after successful strategic `Fertig`. Because storage is generic over DraftOrder, current coverage includes colony population assignments, construction queues, research and fleet movement. Ship design saves remain immediate authoritative mutations; diplomacy/war declaration is intentionally not silently converted to a deferred draft. Browser QA on isolated 7176 built a five-order mixed Triangle Round1 draft (population 3/3/2, Scout queue, research, two fleet moves), hard-reloaded twice with all projections restored, then submitted `Fertig` and verified `planning_draft: null` in Round2. Separate live-snapshot/save-file serialization of unsubmitted drafts remains out of scope for this block.

**TACTICAL MOBILE PERFORMANCE / SHIP READABILITY / DAMAGE FEEDBACK IMPLEMENTED / BROWSER-QA GREEN.** Legal movement no longer materializes hundreds of rounded SVG cells; the server-projected legal set is rendered as one crisp green grid path and battlefield clicks remain authority-gated. Follow-up original-data review corrected the movement interpretation: a pristine Frigate uses drive max combat speed (Nuclear 20, Fusion 22); the 10/12 values are engine-damage minima, so the temporary MinSpeed change is reverted. Tactical ship art is native centered nested SVG with a tight viewBox. New-game Scout designs now start with a fully resolved persistent ShipVisualGenome v4 / VisualRevision 1, and every starting Scout clones that exact design genome instead of browser fallback. Isolated fresh Triangle/Game One QA on 7178 proves the persisted Scout genomes render in the Galaxy fleet picker. Beam impact timing remains slower with authoritative layer-damage numbers. See SLICE_15_6_GATE3_TACTICAL_MOBILE_PERFORMANCE_DAMAGE_FEEDBACK_2026-09-12.md and SLICE_15_6_GATE3_STARTING_SCOUT_VISUALS_SPEED_CORRECTION_2026-09-12.md.

**TACTICAL DIRECT SELECTION / HULL SCALE / FINISH / DAMAGE DWELL IMPLEMENTED / REAL-BATTLE BROWSER-QA GREEN.** Own ships can be selected and camera-focused directly; the selected cell is a filled blue 1x1 square, occupied cells are red outlines, and the single green reachable grid remains server-projected for the actual active ship. Circle selection chrome is removed. Persisted visual hull identity now controls Tactical scale from Scout .40 through Doom Star 1.00 using a geometry-tight SVG viewBox and exact full-cell hit targets. HUD lists only unfinished own activations. Fertig is the visible label for the existing authoritative battle.end_activation command; no React initiative switching is fabricated. Damage numbers stay readable for 4 seconds. Isolated Triangle Battle #1 browser QA on 7179 proved selection/focus, 20/20 Nuclear movement, 515 legal moves, cell colors, roster removal after Finish and automatic next-active selection. See SLICE_15_6_GATE3_TACTICAL_SELECTION_SCALE_FINISH_DAMAGE_DWELL_2026-09-12.md.

**TACTICAL RETREAT CONFIRMATION GUARD IMPLEMENTED / REAL-BATTLE BROWSER-QA GREEN.** A canonical Triangle incident proved one-click attle.retreat could catastrophically remove the Human fleets when no other owned colony was available as a retreat destination. Tactical Retreat is now explicitly two-stage: first click opens a modal only, with a no-retreat-destination fleet-loss warning; Abbrechen/Escape/backdrop are zero-mutation; only R�ckzug best�tigen submits the authoritative retreat command. See SLICE_15_6_GATE3_TACTICAL_RETREAT_CONFIRM_GUARD_2026-09-13.md.

**TACTICAL WAIT / DONE AUTHORITATIVE REORDERING IMPLEMENTED / CURRENT-BATTLE BROWSER-QA GREEN.** Added attle.wait_activation: Wait changes active ship among alive unfinished same-seat ships without marking Done, restoring movement, or reloading weapons. Direct own-ship click invokes the same server contract; the player can return to a waited ship with its exact remaining movement/readiness until explicitly pressing Fertig. End-activation progression now wraps through unfinished initiative entries and starts a new round only when all live ships are complete. Isolated replay from canonical Battle#1 proved 18/20 movement survives Wait away/back and Laser Scout #35 becomes directly selectable with its two authoritative fire targets. See SLICE_15_6_GATE3_TACTICAL_WAIT_DONE_REORDER_2026-09-13.md.

**TACTICAL AUTO-FINISH ON RESOURCE EXHAUSTION IMPLEMENTED / BROWSER-QA GREEN.** Successful move/fire commands now auto-complete the active ship only when movement is exhausted and no weapon remains ready. Unarmed ships auto-finish at 0 movement; armed ships remain active at 0 movement until their final ready weapon is spent. The server emits ctivation_auto_ended and advances through the same authoritative activation/round path as explicit Fertig. Isolated current-Battle QA proved a legal 20-point Scout move to 0/20 automatically completed Scout23 and activated Scout24 at 20/20 with no extra click. See SLICE_15_6_GATE3_TACTICAL_AUTO_FINISH_2026-09-13.md.

**PLANNING DRAFT REVISION REBASE FIX IMPLEMENTED / EXACT MOBILE-NAVIGATION REGRESSION GREEN.** Immediate authority mutations during Planning (notably Ship Builder gameplay + visual saves) now rebase and server-validate compatible stored planning drafts instead of letting revision mismatch delete them. Late stale draft saves are also rebound to the current Planning revision and validated. Exact Triangle browser test preserved 5 Farmer / 3 Worker / 0 Scientist plus Scout r2 + Colony Base across Browser Back, page reload and a fresh browser reconnect. See SLICE_15_6_GATE3_PLANNING_DRAFT_REBASE_2026-09-13.md.

**TACTICAL FOCUS + UNIFIED CONTROLS + READINESS ROSTER IMPLEMENTED / ISOLATED CURRENT-BATTLE QA GREEN.** Tactical starts and follows the authoritative active ship at zoom 3.2 instead of fitting the full own formation. Scan and all activation/weapon controls now share one compact control card with icons. Noch verf�gbare Schiffe shows movement current/max and authoritative ready/total weapon count. Distinct weapon slots remain selectable through server-projected legal_fire_actions; partial split-fire within one multi-count weapon slot is deferred until the server contract supports partial consumption. See SLICE_15_6_GATE3_TACTICAL_FOCUS_CONTROLS_READINESS_2026-09-13.md.

**TACTICAL ACTION ROW + RESPONSIVE ROSTER + BLOCKED-CELL CLARITY IMPLEMENTED / MOBILE+DESKTOP QA GREEN.** Zur�ck zur Begegnung removed from Tactical; action row now contains only Scan, Wait, Finish and Retreat. Ship roster uses responsive auto-fill on larger screens and exactly 4 columns on <=720px, with max 2 visible rows then vertical scroll. Blocked occupied cells are now translucent red filled rather than outline-only. See SLICE_15_6_GATE3_TACTICAL_ACTION_ROSTER_BLOCKED_2026-09-13.md.
