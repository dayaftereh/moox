# Active research

This file is the authoritative recovery point for ongoing MOOX reverse engineering.

Read this file before broad exploration. Update it whenever the active objective, blocker, checkpoint, or dirty-tree state changes.

## Status

**COMPLETED - 2026-08-27**

## Current objective

Planet-class normalization has been completed as a tested, documented checkpoint. The next project objective is engineering Phase 1: the deterministic simulation skeleton.

The current code indicates the intended slice is limited to original-observed planet classification/economic primitives:

- planet sizes,
- mineral classes,
- gravity classes,
- climate classes,
- original size-generation thresholds,
- original mineral-extraction values,
- original food-per-farmer values,
- English localization keys/provenance.

## In scope

- `internal/moo2data/planet_classes.go`
- `internal/ruleset/planet_classes.go`
- narrowly required tests for these files,
- normalized `planet_classes.json` if the decoder is ready,
- English localization keys generated by the decoder,
- research documentation for the exact original evidence used,
- CLI wiring only if required to generate the normalized artifact.

## Out of scope for this ticket

- planet graphics / `PLANETS.LBX` semantic mapping,
- galaxy generation as a whole,
- population-capacity formulas not already part of the current decoder,
- terraforming behavior,
- specials / artifacts / natives,
- tactical or strategic ship work,
- reopening building, race, technology or ship mappings that are already committed,
- broad searches for unrelated planet mechanics.

Record discoveries about those topics under `Later / follow-up`; do not branch into them during this objective.

## Existing dirty-tree state

The following files existed uncommitted when the working rules were introduced on 2026-08-27:

- `internal/moo2data/planet_classes.go`
- `internal/ruleset/planet_classes.go`
- `tmp_estring_probe.go`
- `tmp_planet_probe.go`

These files are assumed to belong to the active planet-class investigation. They must be reviewed before modification or cleanup. The two `tmp_*` probes should only be deleted once their useful findings have been materialized into code/tests/docs.

### Current dirty-tree test effect

go test ./... currently fails at the repository root because 	mp_estring_probe.go and 	mp_planet_probe.go are both package main programs and both define main. This failure predates the working-rules checkpoint and is quarantined with the planet-class work. Package tests under cmd/... and internal/... remain the appropriate unaffected validation until the probe findings are materialized and the probes are deliberately removed or relocated as part of the active planet ticket.

## Proven closed milestones ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â do not rediscover without contradictory evidence

### Race design / races

Closed through committed normalized race traits, preset races, localization and semantic race assets.

### Buildings / technologies

Closed identity/index/technology-link and colony-graphics mapping milestones include:

- `3a9d8ab feat: normalize MOO2 technologies`
- `cf0303f feat: read building table from original executable`
- `425dab5 feat: map building colony graphics from original executable`

Do not revisit `_buildings`, the BLDG archive formula or the 6x6 building-frame formula unless new evidence contradicts them.

### Player ship hull graphics

Closed milestones:

- `980098c feat: normalize player ship hulls`
- `7e2125e feat: map strategic ship graphics from original executable`
- `53d3a6b feat: map tactical ship frames from original executable`

Already proven/documented in `docs/research/MOO2_SHIP_GRAPHICS.md`:

- six military hull identities,
- strategic `SHIPS.LBX` player-color formula,
- Colony / Outpost / Transport strategic picture IDs,
- tactical `CMBTSHP.LBX` player-color formula,
- strategic picture ID -> tactical picture ID for standard military hulls,
- 20 tactical frames = 5 folded orientations x 4 neutral animation phases.

Do **not** restart broad `SHIPS.LBX` / `CMBTSHP.LBX` exploration as part of the current planet ticket.

## Last good research checkpoint

`53d3a6b feat: map tactical ship frames from original executable`

The working tree after that commit contains the planet-class work listed above.

## Current evidence already visible in the planet-class work

The uncommitted decoder already names several intended original evidence paths:

- `ESTRINGS.LBX` planet-class names,
- original executable planet-class string mapping,
- original executable planet-size generation,
- original executable mineral extraction,
- original executable food per farmer.

The code includes hash constants for the relevant original function ranges. These must be validated by tests/real normalization rather than assumed correct merely because they are present in the draft.

## Next exact action

The active implementation/research lane is now **Research / multi-Technology TechFields**. The Food/Freighter/Starvation Economy checkpoint is closed for this pass and documented in `FOOD_FREIGHTER_LOGISTICS_2026-08-27.md`.

### Current lane - Research / multi-Technology TechFields

`GameSession.ResearchChoices` is the server-authoritative TechField frontier and `empire.select_research` currently accepts only `tech_field_id`. The next correction is to stop treating every Technology inside a researched field as an unconditional acquisition.

Research tasks, in order:

1. verify ordinary-race acquisition semantics for TechFields containing multiple Technologies,
2. verify Creative behavior and whether all Technologies in the field are acquired automatically,
3. verify Uncreative behavior, including whether the available Technology is preselected/randomized and when that selection becomes visible,
4. determine whether the Technology choice occurs when research starts, while it progresses or at breakthrough/completion,
5. represent the proven policy explicitly in server-authoritative `ResearchChoice` / `ResearchState` data rather than trusting client-selected Technology IDs,
6. keep Human UI, built-in AI and future remote/MCP agents on the exact same legal-action surface,
7. investigate active-project switching/cancelling and progress retention/loss,
8. model hyper-advanced repeated-field level/cost state separately,
9. return to Advanced-start randomized/race-aware technology grants after those runtime semantics exist.

### Turn-order conflict to resolve while researching Research timing

A secondary StrategyWiki calculations reference explicitly described as checked under MOO2 1.31 gives an original sequence with Population growth/starvation before resource generation and Research completion. The current MOOX runtime intentionally still has Construction/Research consume the pre-Population-transition output.

Do not silently change this order from a single secondary source. While researching the timing of multi-Technology choice/completion, seek stronger original-observed or independent evidence for the 1.31 strategic turn sequence. The Food/Freighter phase is isolated so its placement can be corrected later without redesigning the protocol.

### Parked Economy follow-ups

The following are intentionally parked while Research is active:

- Freighter Fleet acquisition/build legal action (5 Freighters / 50 PP is normalized, but acquisition is not yet wired),
- Treasury settlement of Freighter operating cost and surplus-Food income,
- blockade effects on Food/Production and transport eligibility,
- Population transport and its competition for the Freighter pool,
- exact original allocation priority when too few Freighters exist,
- Housing / Cloning Center / medicine growth modifiers,
- Biospheres / Advanced City Planning / terraforming capacity transitions,
- race-aware Population cohorts.

Architecture constraint: continuous strategic quantities remain domain-native `float64` per `ADR-0003-domain-native-float64.md`; discrete counts such as Empire Freighters remain discrete. Clients never receive authority to mutate hidden/full GameState or submit arbitrary Technology ownership.
## Exploration budget

Budget for the current investigation path: **10 consecutive search/inspection actions without materialized progress**.

Current counter at creation of this file: **0 / 10**.

Reset the counter only when a finding is materialized in code/data/test/docs or a precise blocker is recorded.

## Later / follow-up

- semantic planet graphics from `PLANETS.LBX`,
- planet population-capacity formulas,
- specials/artifacts/native-life data,
- unexplained strategic/tactical ship picture slots that were deliberately left pending,
- additional technology effects/research-cost data.

## Checkpoint template

Before changing this objective, update:

- **Result:** completed / blocked
- **Evidence added:**
- **Artifacts added/changed:**
- **Tests/checks:**
- **Commit:**
- **Next objective:**
- **Dirty tree after checkpoint:**
